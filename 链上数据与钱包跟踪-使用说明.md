# 链上数据 · 数据转换 · 钱包跟踪 使用说明

> 整理日期: 2026-09-04 · 适用目录: `I:/plot/poly_data/`
> 环境: Git Bash + `./.venv/Scripts/python.exe`（与 poly_data 共用 venv, 已含 polars/pandas/pyarrow/hypersync）
> 代理: 拉官方 API 需 `http://127.0.0.1:10808` 在线

---

## 0. 三块东西的关系（一图流）

```
                 ┌─────────────────────────────────────────────┐
                 │  Polymarket CTF Exchange V2 (0xe111...)      │  链上撮合合约
                 └────────────────────┬────────────────────────┘
                                      │ HyperSync 流式采 OrderFilled 事件
              ┌───────────────────────▼───────────────────────┐
              │  ① 链上采集 update_chain.py → data/order_filled/ │  raw 分块 parquet (32GB)
              │     每次结束写 data/order_filled_state.json       │
              └──────────────┬──────────────────┬──────────────┘
                             │ join markets     │
              ┌──────────────▼──────────────┐   │  wallet_tracker annotate
              │ ② process_live.py           │   │  按钱包 hash 判 M/T
              │ → processed/trades/ (13GB)  │   ▼
              │   labeled trades(回测数据)   │  ┌────────────────────────────┐
              └─────────────────────────────┘  │ ③ wallet_tracker/cli.py      │
                                               │ → data/tracked_wallets/{地址}/ │  钱包 M/T 画像
                                               └────────────────────────────┘
```

- **① 链上采集**：谁在链上做了什么撮合（原语）
- **② 数据转换**：把链上原语 join 成"人能看懂的成交记录" → 喂回测
- **③ 钱包跟踪**：盯某个地址，标出每笔是 **Maker 还是 Taker**

---

## 1. ① 链上数据（order_filled）

### 1.1 文件位置与格式
```
poly_data/data/order_filled/
  part_00000000.parquet ~ part_00001176.parquet    ← 分块, 2026-09-04 共 1177 块, 32GB
```
- **Parquet 分块**：每块固定 **50 万行**（`ROWS_PER_PART`，最后一块为余数），**ZSTD 压缩**
- 每块原子写（先 `.tmp` 后 `os.replace`），中断不留半文件 → 可断点续跑
- **10 列**：

| 列 | 含义 |
|---|---|
| `timestamp` | 链上块时间(unix 秒) |
| `maker` / `taker` | 挂单方 / 吃单方 地址(小写 0x) |
| `makerAssetId` / `takerAssetId` | 两侧资产 token id（`0`=USDC, 其它=CTF 结果 token） |
| `makerAmountFilled` / `takerAmountFilled` | 两侧成交数量(×10⁶ = 实际数量/美元) |
| `transactionHash` | 链上交易哈希(每行可能同 tx=一笔交易里多笔撮合碎片) |
| `orderHash` | 订单级哈希 |
| `fee` | 链上手续费(×10⁶) |

### 1.2 采集程序 `update_utils/update_chain.py`
- **数据源**：仅采 **v2 撮合合约 `0xe111180000d2663c0091e4f400237545b87b996b`** 的 OrderFilled 事件（HyperSync, 需 `.env` 里 `HYPERSYNC_API` token）
- **工作原理**：从 `V2_GENESIS_BLOCK`(84_902_353) 或 `data/cursor_state.json` 断点，流式拉到**链顶 - 20 块**(reorg buffer)
- **断点**：`data/cursor_state.json` = `{last_block, part_index}`，只有整块 flush 才推进，崩了从 last_block 重拉不重不漏
- **每次结束自动写** `data/order_filled_state.json`（新鲜度状态）：
```json
{
  "start_time_unix": 1775220779, "start_time_utc": "2026-04-03 12:52:59",  // 数据绝对起点
  "last_time_unix": 1788455125,  "last_time_utc": "2026-09-03 17:05:25",   // 已覆盖到(新鲜度)
  "last_block": 93168845,
  "updated_at_utc": "..."                                                  // 本次运行墙钟
}
```

### 1.3 覆盖范围（重要边界, 已 RPC 实证）
- v2 合约 2026-04-03 部署，但 **04-03~04-22 基本空转**（25 天仅 ~1300 行）
- **实质数据自 ~2026-04-23 起**；**2026-04-28 v1→v2 迁移**后 v2 成为主撮合
- 04-28 前钱包交易大多发生在 **v1 合约 `0x4bfb41d5b3570defd03c39a9a4d8de6bd8b8982e`** → 本库查不到（annotate 标 **V1**，永久无法标 M/T）
- 链上 archive 天然滞后 ~1h（reorg buffer + HyperSync 高度）→ 覆盖点永远比墙钟旧 ~1h，属正常

### 1.4 怎么更新
```bash
cd /i/plot/poly_data
uv run poly-data        # 完整管线 markets→chain→process（process 很吃内存, 见下）
# 只更新链上(chain, 不跑 process):
./.venv/Scripts/python.exe update.py        # 同上=完整管线
```
> ⚠️ 2026-09-04 曾遇 `uv run` 卡死（解析环节无子进程）→ 可用 venv python 直跑绕开。

---

## 2. ② 数据转换程序 process_live.py（⚠️ 峰值 ~20GB 内存）

### 2.1 它是干嘛的
把链上**原始撮合** join **市场元数据**，转成带市场上下文的 **labeled trades**（回测/策略研究数据）。

### 2.2 文件位置与格式
```
poly_data/processed/trades/
  trades_part_00000000.parquet ~ trades_part_00001171.parquet   ← 2026-09-04 共 1172 块, 13GB
```
- 一个 order part → 一个 trades part（1:1 编号），**ZSTD**，断点幂等续跑
- **12 列**：`timestamp / market_id / maker / taker / nonusdc_side / maker_direction / taker_direction / price / usd_amount / token_amount / fee / transactionHash`

### 2.3 工作原理（update_utils/process_live.py）
1. 读全部 order part + `markets.csv`（`get_lean_markets`）
2. `pending = order part 里还没有对应 trades part 的`（差集）→ **只处理缺失的，已处理的不动**
3. 每 part：去掉撮合合约 0xe111 冗余记账行(防重复计数) → 按 token id join 市场 → 标 USDC/结果侧 → 算 BUY/SELL 方向、price、usd/token 金额
4. 原子写（tmp + replace），崩了重跑不重不漏
5. **可选过滤**：环境变量 `MARKET_SLUG_RE`（正则）只保留指定市场 slug（如 crypto updown），空=全市场
6. 若遇 token 不在 markets.csv → 自动去 Gamma 批量补拉

### 2.4 为什么吃内存 & 怎么安全跑
- join + polars 全量在内存重建，**峰值实测 ~20GB**
- **安全跑法**：低峰时段、别同时开大程序；必要时设 `PROCESS_CHUNK_SIZE` 分块
- 想全量重建：删 `processed/trades/` 再跑；或配合 `MARKET_SLUG_RE` 只留子集
- 单独跑（只补 process，不重采链上）：
```bash
cd /i/plot/poly_data
PYTHONUTF8=1 ./.venv/Scripts/python.exe -c "from update_utils.process_live import process_live; process_live()"
```

### 2.5 谁在用它
`processed/trades/` 下的 labeled trades 供 **回测、策略分析、updown 研究** 使用。**与钱包 M/T 标注无关**（标注只读 order_filled）。

---

## 3. ③ 钱包跟踪 wallet_tracker（原 user-trade-tracker, 已并入 poly_data）

### 3.1 代码位置
```
poly_data/wallet_tracker/
  cli.py                 命令行入口
  config.toml            配置(见 3.5)
  src/user_tracker/      config / store / api / annotate / freshness / query
  README.md              详细用法
```

### 3.2 数据位置（统一收在 poly_data/data/ 下）
```
poly_data/data/tracked_wallets/
  wallets.json                           已登记钱包 {地址: {start, added}}
  0x361528e242bc6cc789ac8da6fd5cb98046178fdf/   每钱包一个独立目录
    activity.jsonl       原始逐行活动(增量 append, ~207MB)
    activity.parquet     查询用(23列, 含 utc_time/outcome_norm)
    activity_mt.parquet  annotate 后(24列, 多 role)
```
> 每钱包数据独立存储不混合；此目录已被 git 忽略（不进版本库）。

### 3.3 用法（PY = /i/plot/poly_data/.venv/Scripts/python.exe）
```bash
cd /i/plot/poly_data/wallet_tracker

# 登记并全量采集(起点自动 clamp 到 4/29; 终点=链上覆盖点)
$PY cli.py add <地址> [起始日]

# 增量更新(幂等; 只拉到链上覆盖点, 不超链上 → 无缝隙 NA)
$PY cli.py update [--force]

# M/T 标注(读 data/order_filled, 需链上已追平)
$PY cli.py annotate <地址>

# 查询
$PY cli.py query summary <地址>
$PY cli.py query market --slug btc-updown-5m-xxx <地址>
$PY cli.py query hash <txhash> <地址>
$PY cli.py query day <日期> <地址> | query daily <地址>
$PY cli.py list
```

### 3.4 工作原理
1. **采集(api.py)**：调官方 `data-api.polymarket.com/activity`，逐天分页拉，按 `(type,timestamp,transactionHash)` 幂等去重；JSONL 增量 append，再重建 parquet。
   - `/activity` 是**撮合碎片级**（transactionHash 每行唯一=真链上 tx，官方"笔数"用 `/trades` 聚合口径）
2. **采集窗口规则(v0.3)**：
   - **起点** = clamp 到 v1 截止（默认 `2026-04-29`）→ 更早只标 V1，不拉
   - **终点** = 链上 `order_filled_state.last_time`（精确到秒）→ 钱包数据**不比链上新** → annotate 无缝隙 NA
3. **M/T 标注(annotate.py)**：全库扫 order_filled（磁盘侧 filter `maker==w | taker==w`，**勿 to_table 全量**），排除撮合合约 0xe111 对手行 → 该钱包在 maker 列=M、taker 列=T
   - **role 三类**：`M/T` 正常 | `V1`（v1 截止前, 永久不标, 扫描直接跳过）| `NA`（v1 后缺口/缝隙, 补链上数据重跑可回填）
4. **新鲜度检查(freshness.py)**：update 前读 `order_filled_state.json`，检查**链上是否停更 > `stale_threshold_hours`(默认 6h)** → 是则提示先 `uv run poly-data`（`--force` 绕过）。链上正常滞后 ~1h 不拦。

### 3.5 config.toml 关键项
```toml
[tracker]
default_start = "2026-01-01"      # 默认起点兜底(会被 v1 clamp)
lookback_days = 2                 # 增量回看补漏天数
require_chain_fresh = true        # update 前强制查链上停更
stale_threshold_hours = 6         # 链上停更多久算陈旧

[annotation]
v1_cutoff = "2026-04-29T00:00:00Z"  # v1→v2 截止; 更早标 V1, 调早可允许标注

[proxy]  url = "http://127.0.0.1:10808"   # 拉官方 API 走代理
[poly_data]  match_contract = "0xe111..."  # 撮合合约(标注忽略其行)
[api]  base = "https://data-api.polymarket.com/activity"  page = 500
```

### 3.6 当前钱包状态（2026-09-04 实测）
```
ohioism 0x3615...: TRADE 198,314 | role M 143,087 / T 39,283 / V1 15,944 / NA 0
```

---

## 4. 推荐日常流程（三块协作）

```bash
# ① 更新链上 → 自动刷 order_filled_state.json
cd /i/plot/poly_data
./.venv/Scripts/python.exe update.py        # 或 uv run poly-data(全流程, 注意 process 内存)

# ② (可选) 若需要最新回测数据 → 补 process
PYTHONUTF8=1 ./.venv/Scripts/python.exe -c "from update_utils.process_live import process_live; process_live()"

# ③ 更新钱包 → 拉到链上覆盖点(无缝隙) → 标注 M/T
cd wallet_tracker
$PY cli.py update && $PY cli.py annotate 0x361528e242bc6cc789ac8da6fd5cb98046178fdf
```

---

## 5. 踩坑记录（务必遵守）

- **⚠️ 内存红线**：`order_filled` 32GB/数亿行，**禁止 `pyarrow dataset.to_table()` 全量物化/转 set**（曾内存打满崩掉 VS Code）。必须磁盘侧 filter；查 hash 命中用 `activity_mt.parquet` 的 role 列
- **process_live 峰值 ~20GB**：低峰跑，盯内存
- **时间精度**：parquet 里 `utc_time` 是 `datetime64[ms]`（毫秒）**非纳秒**——勿用 `astype(int64)//1e9` 取秒，用 `pd.Timestamp(cut, unit="s")` 直接比 datetime
- **UTF-8**：Windows 默认 GBK 无法编码 emoji/箭头（🔄→↔✅ 会 `UnicodeEncodeError`）→ 各入口已强制 `stdout.reconfigure(encoding="utf-8")`；自己写脚本跑长任务建议加 `PYTHONUTF8=1`
- **Git Bash 路径**：终端用 `/i/plot/...`；Windows python 代码内用 `I:/plot/...`
- **数据红线**：`poly_data/data/order_filled`、`processed/trades`、`tracked_wallets` 均为重要数据，删除/重建前先备份并确认

---

## 6. 覆盖日期速查
| 时间 | 事件 |
|---|---|
| 2026-04-03 | v2 合约部署(基本空转) |
| ~2026-04-23 | v2 实质订单开始 |
| 2026-04-28 | v1→v2 迁移(15:14 起 v2 主撮合) |
| **2026-04-29** | **钱包可正常标 M/T 的起点**(v1_cutoff 默认值) |
| 2026-09-03 17:05 UTC | order_filled 最新覆盖点(本次更新后) |
